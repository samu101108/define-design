{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper section-container my-5">
		<div class="row justify-content-center justify-content-md-space-between">
		{% for original in originais %}
			<div class="col-sm-6 col-md-4 col-lg-3">
				<div class="d-flex flex-column align-items-center">
					<a href="{{ original.link }}"><img src="{{original.thumbnail }}" style="width: 400px;" alt=""></a> <br>
					<a href="{{ original.link }}">{{original.title}}</a> <br>
				</div>
			</div>
		{% endfor %}
		</div>
	</div><!-- /content-wrapper -->
{% endblock %}
{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
			<section class="article-content">
				<div class="featured-img d-flex align-items-end pb-4 d-block d-md-none" 
				style="background-image: url('{{ post.thumbnail.src }}')">
					<h1 class="article-h1 text-white px-xl-4 mb-md-5">{{ post.title }}</h1>
				</div>
				<div class="article-body">
					{# <p class="blog-author">
						<span>Por</span><a href="{{post.author.path}}"> {{ post.author.name }} </a>
						<span>&bull;</span> <time datetime="{{ post.date|date('Y-m-d H:i:s') }}">{{ post.date }}</time>
					</p> #}
					<div class="row">
						<div class="pe-0 pe-md-5">
							<div class="">
								<article>
									<img class="featured-img pb-4 pe-4 d-none d-md-block" src="{{ post.thumbnail.src }}" />
									<h1 class="article-h1 px-xl-4 mb-md-5 d-none d-md-block">{{ post.title }}</h1>
									{{post.content}}
								</article>
							</div>
						</div>
						<aside class="col-lg-4 w-25 border border-light bg-dark text-white ps-5 pe-4 d-none">
							<div class="pt-4">
								<p>Lorem ipsum, dolor sit amet consectetur adipisicing elit. Velit suscipit cum, laboriosam vero 
								sit minus saepe quasi consequatur alias? Commodi praesentium, vel eos quod ipsa non nemo architecto
								deleniti, iusto omnis nesciunt ea, recusandae at unde voluptas qui beatae necessitatibus est
								asperiores eveniet provident? Ab vitae aliquid consectetur ut deleniti ipsa natus neque 
								reprehenderit?</p>
							</div>
						</aside>
					</div>
				</div>
			</section>

			<!-- comment box -->
			{# <section class="comment-box">
				<!-- comments -->
				<div class="comments">
				    {% if post.comments %}
					<h3> comments </h3>
					{% for cmt in post.comments %}
					    {% include "comment.twig" with {comment:cmt} %}
					{% endfor %}
				    {% endif %}
				</div>

				{% if post.comment_status == "closed" %}
					<p> comments for this post are closed </p>
				{% else %}
				    <!-- comment form -->
				    {% include "comment-form.twig" %}
				{% endif %}
			</section> #}
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}
